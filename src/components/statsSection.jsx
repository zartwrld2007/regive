import "./../styles/stats.css";
import StatCard from "./StatCard.jsx";

export default function StatsSection() {
  return (
    <section className="stats-container">
      {/* Stat cards removed (duplicate) - handled in hero section */}
    </section>
  );
}
